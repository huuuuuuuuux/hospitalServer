<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="hospitalServer.dao.DoctorDao" >

    <select id="getDrByDept" parameterType="java.lang.Integer" resultType="hospitalServer.bean.Doctor">
        SELECT * FROM doctor WHERE dept_id=#{deptId}
    </select>

</mapper>